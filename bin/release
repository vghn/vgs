#!/usr/bin/env bash
# Release tasks

# Immediately exit on errors
set -euo pipefail

# Load functions
# shellcheck disable=1090
. "${APPDIR}/load"

vgs_release \
  --sign \
  --type "${1:-patch}" \
  --github-url 'https://github.com/vghn/vgs'
