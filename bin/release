#!/usr/bin/env bash
# Release tasks

# Immediately exit on errors
set -euo pipefail

# Load functions
# shellcheck disable=1090
. "${APPDIR}/load"

vgs_release -t "${1:-patch}" -u 'https://github.com/vghn/vgs'
